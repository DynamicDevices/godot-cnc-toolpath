# Archived toolpaths

Raster and waterline **toolpath** strategies were frozen on

`archive/raster-waterline-toolpaths`

(tip: main at `8f4fdb3`, including BarMesh viz plus the old bake pipeline).

**main** is tool-surface only (`barmesh/`) until that surface is right. Do not
port toolpaths back until Julian says so.
