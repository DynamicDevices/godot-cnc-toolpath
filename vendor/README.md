# Vendor

- `barmesh/` — Julian Todd’s Python CAM / ball-offset mesh code (MIT).
  Shared as the calculation reference for the Godot toolpath bake.
  Do not treat editor GDScript as SoT for the geometry math until ported.
